const squarefunc = x => x * x;

const multiplyfunc = (x, y) => x * y;

const sumfunc = (x, y) => x + y;

const minusfunc = (x, y) => x - y;

module.exports = {
  squarefunc,
  multiplyfunc,
  sumfunc,
  minusfunc
};
